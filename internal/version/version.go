// Package version is the single source of truth for Peapod's version string.
// The CLI, the MCP server and the app bundle (via build.sh) all read it here.
package version

// Version is the current Peapod release.
const Version = "0.3.0"
